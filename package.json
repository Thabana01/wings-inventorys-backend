{
  "name": "wings-inventory-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build-frontend": "cd frontend && npm install && npm run build",
    "postinstall": "npm run build-frontend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": "22.x"
  }
}
